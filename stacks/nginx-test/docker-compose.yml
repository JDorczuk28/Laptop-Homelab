# Basic Nginx test container
# Access from another device on the LAN at:
# http://SERVER_IP:8080

services:
  nginx:
    image: nginx:latest
    container_name: nginx-test
    ports:
      - "8080:80"
    restart: unless-stopped
